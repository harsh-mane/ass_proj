/// Model class representing image data.
class ImageModel {
  /// The URL of the image.
  String imageUrl;

  /// Creates an [ImageModel] with an optional [imageUrl].
  ImageModel({this.imageUrl = ''});
}
